# Isolated requirements for the Airflow orchestration layer ONLY (Phase 10).
#
# Deliberately NEVER installed alongside requirements/base.txt or dev.txt --
# confirmed while building this phase that apache-airflow's own pinned
# constraints force sqlalchemy down to 1.4.x and a typing_extensions version
# incompatible with this repo's pydantic>=2/fastapi stack, breaking it
# outright. See docker/airflow.Dockerfile and
# scheduler/airflow/dag_common.py's module docstring.
#
# Installed ONLY into the dedicated Airflow image (docker/airflow.Dockerfile),
# which never installs requirements/base.txt or dev.txt.
--constraint https://raw.githubusercontent.com/apache/airflow/constraints-2.10.4/constraints-3.12.txt
apache-airflow==2.10.4
apache-airflow-providers-docker
